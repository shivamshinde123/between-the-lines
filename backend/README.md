# Backend

This folder is reserved for backend-specific code as the project grows.

For now, server-side behavior is still handled inside the Next.js app in `frontend/`. When the project introduces dedicated backend modules or services, they should live here instead of being mixed into the frontend tree.
